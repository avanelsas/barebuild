name: release

on:
  push:
    tags:
      - 'v[0-9]*'

permissions:
  contents: write

jobs:
  release:
    name: Build, Test & Publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Derive version from tag
        id: version
        run: |
          TAG="${GITHUB_REF_NAME}"
          VERSION="${TAG#v}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Verify versions match the tag
        run: |
          V="${{ steps.version.outputs.version }}"
          status=0
          grep -Eq "^\(def version +\"${V}\"\)" build.clj || { echo "build.clj version != ${V}" >&2; status=1; }
          grep -q "target/barebuild-${V}.jar" deps.edn      || { echo "deps.edn artifact != ${V}" >&2; status=1; }
          grep -q "\"version\": \"${V}\"" package.json      || { echo "package.json version != ${V}" >&2; status=1; }
          exit $status

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'

      - uses: actions/setup-node@v6
        with:
          node-version: '22'

      - uses: DeLaGuardo/setup-clojure@13
        with:
          cli: 'latest'

      - name: Setup clj-kondo
        run: |
          curl -sLO https://raw.githubusercontent.com/clj-kondo/clj-kondo/master/script/install-clj-kondo
          chmod +x install-clj-kondo
          sudo ./install-clj-kondo --dir /usr/local/bin --version 2026.01.19
          clj-kondo --version

      - name: Setup Babashka
        run: |
          curl -sLO https://raw.githubusercontent.com/babashka/babashka/master/install
          chmod +x install
          sudo ./install --dir /usr/local/bin
          bb --version

      - name: Install deps
        run: npm ci

      - name: Lint
        run: clj-kondo --lint src test demo/src demo/test demo/dev-server

      - name: Unit tests (Node)
        run: npm test

      - name: Browser tests (headless Chrome)
        run: npm run test:browser
        env:
          CHROMIUM_FLAGS: "--no-sandbox"

      - name: Server handler tests
        run: npm run test:server

      - name: Release build (Closure Advanced)
        run: npm run build

      - name: Build and Publish to Clojars
        run: |
          clojure -T:build jar
          clojure -X:deploy
        env:
          CLOJARS_USERNAME: ${{ secrets.CLOJARS_USERNAME }}
          CLOJARS_PASSWORD: ${{ secrets.CLOJARS_PASSWORD }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: "BareBuild ${{ steps.version.outputs.version }}"
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
